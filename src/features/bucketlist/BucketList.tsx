// Placeholder — the full Bucket List tab (collapsible category sections,
// quick-add, filter/sort, tags) is being built in the next step of the
// Phase 6 redesign. This keeps the app compiling and the tab reachable
// in the meantime.
export function BucketList() {
  return (
    <div className="space-y-2">
      <h2 className="font-display text-lg text-moon">Bucket List</h2>
      <p className="text-sm text-moon-dim">
        Coming next: collapsible sections for Travel Destinations, Books to Read, Shows to Watch,
        and Movies to Watch, with quick-add and per-category filtering/sorting by tag.
      </p>
    </div>
  )
}
