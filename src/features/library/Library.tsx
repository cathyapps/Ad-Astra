// Placeholder — the full Library tab (Currently Reading / My Library
// bookshelf + TBR / Metrics with the dynamic chart builder) is being
// built in the next step of the Phase 6 redesign.
export function Library() {
  return (
    <div className="space-y-2">
      <h2 className="font-display text-lg text-moon">Library</h2>
      <p className="text-sm text-moon-dim">
        Coming next: Currently Reading, My Library (bookshelf view + TBR list), and Metrics (the
        dynamic reading-stats chart builder).
      </p>
    </div>
  )
}
